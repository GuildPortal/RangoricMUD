function Panel(tSettings) {
    var vThis = {
        Name: tSettings.Name,
        Description: tSettings.Description,
        Buttons: tSettings.Buttons,
        IsVisible: tSettings.IsVisible
    };
    return vThis;
}